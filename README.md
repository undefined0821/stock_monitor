# 实时股票监控平台 v3

监控你的 A 股持仓，**仅在交易日（周一~周五）运行**。

## 功能
- **🚨 异动提醒（置顶）**：实时滚动预警，置顶展示
- **📈 上证指数 1 小时后趋势预测（最上方）**：每 2 分钟更新，只判「涨/跌」方向 + 概率，并附**大盘分时图**辅助
- **🔥 开盘前涨停趋势（9:25:02 首扫，持续到 9:29 每分钟更新）**：自动扫描**全市场主板**，排除你的持仓/ST/观察池，找出 **5 只**最可能涨停的未持有强势股，并标注涨停概率
  - 筛选条件：主板(排除创业板/北证)、流通市值≥37亿、股价≥8元、排除ST
  - **两段式**：9:25:02 首扫全市场(约1分钟)生成候选池，之后每分钟只对候选池重排，实时快
- **💼 持仓卡片（隐私保护）**：实时价/盈亏 + 🛑止损线 + 🟢补仓区1/2 + ✅止盈线 + ⛰压力位 + 涨停/跌停价 + 异动标签；卡片主色以**成本价 vs 实时价(盈亏)**为准（非昨收）；**浮动盈亏恒基于成本；当日盈亏分段计算**——开盘前持仓部分按上一交易日收盘、今日加仓部分按成本（加仓不抹除加仓前已有收益，也不把加仓亏损算成昨收收益）；右上角显示**所属细分题材涨跌**（题材名+平均涨跌幅+强弱排名）；**新增持仓经东方财富行业+名称自动归类，命中题材即自动显示芯片（无需手动纠错）**；已移除"持仓时长"；顶部「👁 小眼睛」可一键隐藏金额

- **🌐 大盘 & 题材资金动向**：三大指数 + 细分题材涨跌榜（CPO/PCB/纸业/白酒 等）+ 「题材拉/踩指数」（细分主线相对大盘偏离）
- **🔮 尾盘预测（15:00）**：下个交易日涨跌概率（大盘+个股）
- **🚀 尾盘高开潜力（收盘前 8 分钟）**：自动扫描**主板（排除涨停）**，选出 **5 只**下个交易日开盘高开概率最高的股票，并标注高开概率与置信度

## 启动
```bash
bash run.sh          # 前台
# 或后台常驻
nohup bash run.sh > monitor.log 2>&1 &
```
访问 http://localhost:8800

## 关键点说明
- **数据源**：腾讯财经实时行情 `qt.gtimg.cn`（真实数据，无需 token）。本沙箱东方财富/新浪被封，故无法拉取历史K线。
- **涨停扫描的历史条件受限**：你要求的"近一年涨停次数<15、昨日未涨停、5日振幅<17%"需历史数据，本沙箱拿不到，已用**当日实时强度**（距涨停距离+量比+涨幅+委比）替代，并在界面注明。
- **候选池**：全市场主板首扫需约1分钟，构建期间接口即时返回"构建中"，完成后自动填充。
- 修改 `portfolio.json` 的 `take_profit_pct`/`pressure_pct`/`scan` 可自定义止盈/压力/筛选。

> 本平台为个人监控与异动辅助工具，所有预测/概率均基于规则与统计模型，**不构成投资建议**。

---

## 📋 版本更新日志（超简化）

| 版本 | 更新内容 |
|------|----------|
| **v3.9.5** | 当日盈亏分段计算: 开盘前持仓按昨收、今日加仓按成本, 修复加仓抹除加仓前收益的问题 |
| **v3.9.4** | 当日盈亏口径按"最后买入日"判定(今天首买/加仓按成本, 之前买入未动按昨收) + 修复中贝"昨天买今天加仓"仍按昨收的bug |
| **v3.9.3** | 持仓题材芯片全自动：前端新增/存量持仓经东方财富行业+名称自动归类，命中即显示芯片，无需手动纠错 |
| **v3.9.2** | 修复新增持仓当日盈亏误用昨收基准(改默认买入日=今天) + 新增持仓自动识别题材芯片(补医药/CDMO) + 回测卡新增近5次调参变化并移除手动调参 |
| **v3.9.1** | 高开回测卡片精简：仅展示昨日5只开盘实测，版面紧凑化 |
| **v3.9.0** | 持仓卡片改为成本基准配色 + 布局紧凑化 + 移除持仓时长 + 题材(细分)涨跌芯片自动显示 + 题材拉/踩指数细化 |
| **v3.8.0** | 持仓卡片增加所属板块涨跌(角落芯片) + UI 精细化重做 + 每次发版自动推送到 GitHub |
| **v3.7.7** | 尾盘高开潜力改为收盘前 8 分钟自动扫描 |
| **v3.7.6** | 开盘前涨停趋势首扫时间改为 9:25:02 |
| **v3.7.5** | 修正「当日盈亏」算法 |
| **v3.7.4** | 持仓成本精确到小数点后第三位 |
| **v3.7.3** | 修复高开回测「累计数翻倍」显示 bug |
| **v3.7** | 高开回测精度优化（纯启发式，无 AI 模型） |
| **v3.6** | 修复服务反复崩溃/无响应（504）的根因 |
| **v3.4** | 尾盘高开潜力新增回测闭环与权重自优化 |
| **v3.3** | 持仓模块新增前端编辑能力 |
| **v3.2** | 上证指数 1 小时后趋势预测频率提高到 2 分钟 |
| **v3.0** | 尾盘预测自动触发时间改为 **14:50**（原15:00）；新增「🎯 立即预测」按钮（点击即触发一次完整预测，含AI融合，非刷新旧数据）；修复点击后按钮消失 bug（按钮移出卡片常驻）；新增 🌙/☀️ 白天夜间主题切换按钮；持仓数据按券商实际更新；尾盘高开潜力新增「🎯 立即检测」按钮 + 自动触发时间对齐14:50 + **AI精修加超时保护**（120秒未完成自动回退启发式，避免扫描永久卡死） |
| **v2.9** | 尾盘预测模块新增「🔄 手动刷新」按钮（含刷新中状态反馈），预测一般需 10~30 秒 |
| **v2.8** | 三个预测模块精度升级：上证1小时 / 尾盘预测 / 尾盘高开潜力 统一引入**宽度 / 尾盘动向 / 小盘情绪**多因子 + **置信度校准** + AI完整上下文；尾盘高开新增尾盘拉升信号 |
| **v2.7** | 涨停扫描增强：一字板权重下调 + 流通市值区间打分 + 板块共振 + 妖股连板加分；AI prompt 加入市值/共振/封单稳定性上下文，减少一字板陷阱 |
| **v2.6** | 尾盘预测大盘从2因子扩展，涨停打分全部参数可配（`settings.scan`） |
| **v2.0** | 基础框架：异动提醒置顶 / 持仓卡片(止损·补仓·止盈·压力) / 上证1小时预测 / 开盘前涨停趋势 / 大盘板块资金 / 尾盘预测 |

---

## ⚡ 性能基线（实测，沙箱 Ubuntu22.04 / Chromium / Python3.11）

尾盘高开潜力（gapup）已从「约 4 分钟且易卡死」优化到「约 2 秒稳定出结果」：

| 阶段 | 优化前 | 当前 |
|------|--------|------|
| `universe` 全市场扫描（3001 只） | 串行 8 只/请求，约 **173 秒** | 线程池并行 + 批量 40 只，**约 1 秒** |
| `top15` 拉分时算尾盘 | 串行，约 30-60 秒 | 线程池并行，**数秒** |
| AI 精修 | 本地模型逐只推理，**卡死/超时回退** | **默认跳过本地模型**（沙箱无 GPU 不稳定），仅远程 AI（无则回退启发式） |
| 端到端 | ~4 分钟 + 卡死 | **~2 秒**（`done, 5 rows`） |

> 优化点：行情抓取统一走 `fetch_tencent`，内部批量切分 + 线程池并发 + `requests.Session` 复用；全局共享线程池避免线程堆积；`/api/snapshot` 加超时保护防止接口挂起。**本地 AI 精修因沙箱环境不稳定已默认关闭**，如需恢复，配置远程 AI API 即可启用远程精修。

## 目录结构

```
stock_monitor/
├── app.py                  # 主程序：Flask 服务 + 各预测模块 + 行情抓取
├── run.sh                 # 前台启动脚本
├── watchdog.sh            # 看门狗：保活 8800 端口上的服务
├── build_static.py        # 生成自带数据的独立仪表盘 HTML
├── build_static_quick.py  # 快速构建静态快照
├── portfolio.json         # 持仓配置（改完重启生效）
├── all_a_codes.json       # 全市场代码表
├── dashboard_snapshot.html# 快照看板
├── models/                # 本地 AI 模型（Qwen2.5 0.5B / 1.5B，当前未启用）
└── README.md
```

## ⚠️ 部署与数据保护（务必遵守）

`portfolio.json`、`gapup_log.jsonl`、`gapup_stats.json`、`gapup_weights_tuned.json` 是**用户运行时数据**（前端实时编辑 / 服务累积生成），已从 git 忽略，模板为 `portfolio.json.example`。

**发布必须用 `deploy.sh`，不要用 `publish.js` 直发：**

```bash
bash deploy.sh   # 先拉取线上持仓/回测数据 → 再部署，避免覆盖前端改动
```

严禁以下操作（会拿陈旧基线顶掉线上用户数据）：
- ❌ `git checkout -- portfolio.json` / `git commit -A`（会恢复或提交陈旧持仓）
- ❌ 直接 `node .../publish.js --dir .`（整目录覆盖式部署，本地旧文件会顶掉线上）

`/api/portfolio` GET 返回完整持仓（含 `buy_date`），便于 `deploy.sh` 忠实同步还原；服务启动时若数据文件缺失会自动从 `portfolio.json.example` 初始化 / 播种回测基线。

> 本平台为个人监控与异动辅助工具，所有预测/概率均基于规则与统计模型，**不构成投资建议**。
